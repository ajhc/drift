#!/bin/sh 
prefix=@prefix@
exec_prefix=@exec_prefix@
if grep -q '{-!' $2; then
    echo @bindir@/DrIFT $2 -o $3;
    @bindir@/DrIFT $2 -o $3;
else 
    echo "{-# LINE 0 \"$1\" #-}" > $3
    cat $2 >> $3;
fi
